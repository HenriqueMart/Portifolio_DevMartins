import DesktopNavigation from "./DekstopNavigation";
import MobileNavigation from "./MobileNavigation";

const NavBar = () => {
    return(
        <header>
            <DesktopNavigation />
            <MobileNavigation />
        </header>
        
    )
}

export default NavBar;